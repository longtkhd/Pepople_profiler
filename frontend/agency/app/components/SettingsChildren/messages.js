/*
 * SettingsChildren Messages
 *
 * This contains all the text for the SettingsChildren component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SettingsChildren';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SettingsChildren component!',
  },
});
