/*
 * CombinedCustom Messages
 *
 * This contains all the text for the CombinedCustom component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CombinedCustom';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CombinedCustom component!',
  },
});
