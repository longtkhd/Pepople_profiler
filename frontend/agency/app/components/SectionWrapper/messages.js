/*
 * SectionWrapper Messages
 *
 * This contains all the text for the SectionWrapper component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SectionWrapper';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SectionWrapper component!',
  },
});
