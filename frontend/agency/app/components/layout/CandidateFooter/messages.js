/*
 * CandidateFooter Messages
 *
 * This contains all the text for the CandidateFooter component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CandidateFooter';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CandidateFooter component!',
  },
});
