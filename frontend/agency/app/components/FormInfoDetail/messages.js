/*
 * FormInfoDetail Messages
 *
 * This contains all the text for the FormInfoDetail component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FormInfoDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FormInfoDetail component!',
  },
});
