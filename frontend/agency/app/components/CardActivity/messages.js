/*
 * CardActivity Messages
 *
 * This contains all the text for the CardActivity component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CardActivity';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CardActivity component!',
  },
});
