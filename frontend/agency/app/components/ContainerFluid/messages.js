/*
 * ContainerFluid Messages
 *
 * This contains all the text for the ContainerFluid component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ContainerFluid';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ContainerFluid component!',
  },
});
