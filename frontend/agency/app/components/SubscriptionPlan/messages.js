/*
 * SubscriptionPlan Messages
 *
 * This contains all the text for the SubscriptionPlan component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SubscriptionPlan';

export default defineMessages({
  getStarted: {
    id: `${scope}.getStarted`,
    defaultMessage: 'Get Started',
  },
});