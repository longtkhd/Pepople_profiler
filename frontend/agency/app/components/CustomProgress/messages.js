/*
 * CustomProgress Messages
 *
 * This contains all the text for the CustomProgress component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomProgress';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomProgress component!',
  },
});
