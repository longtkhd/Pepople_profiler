/*
 * ButtonClose Messages
 *
 * This contains all the text for the ButtonClose component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ButtonClose';

export default defineMessages({
  close: {
    id: `${scope}.close`,
    defaultMessage: 'Close',
  },
});
