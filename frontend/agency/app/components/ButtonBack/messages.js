/*
 * ButtonBack Messages
 *
 * This contains all the text for the ButtonBack component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ButtonBack';

export default defineMessages({
  back: {
    id: `${scope}.back`,
    defaultMessage: 'Back',
  },
});
