/*
 * RadarChart Messages
 *
 * This contains all the text for the RadarChart component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RadarChart';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RadarChart component!',
  },
});
