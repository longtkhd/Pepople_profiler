/*
 * Paymenteditform Messages
 *
 * This contains all the text for the Paymenteditform component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Paymenteditform';

export default defineMessages({
  saveButton: {
    id: `${scope}.saveButton`,
    defaultMessage: 'Save',
  },
  editButton: {
    id: `${scope}.editButton`,
    defaultMessage: 'Edit',
  },
  cancelButton: {
    id: `${scope}.cancelButton`,
    defaultMessage: 'Cancel',
  },
});
