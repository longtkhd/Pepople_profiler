/*
 * MoreInfo Messages
 *
 * This contains all the text for MoreInfo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MoreInfo';

export default defineMessages({
  moreInfo: {
    id: `${scope}.moreInfo`,
    defaultMessage: 'More Info',
  },
});