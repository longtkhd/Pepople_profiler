// import { selectSetupAgencyPageDomain } from '../selectors';

describe('selectSetupAgencyPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
