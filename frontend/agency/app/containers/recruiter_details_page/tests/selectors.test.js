// import { selectRecruiterDetailsPageDomain } from '../selectors';

describe('selectRecruiterDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
