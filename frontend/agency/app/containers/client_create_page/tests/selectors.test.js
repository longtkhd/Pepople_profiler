// import { selectClientCreatePageDomain } from '../selectors';

describe('selectClientCreatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
