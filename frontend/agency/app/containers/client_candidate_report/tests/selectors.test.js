// import { selectClientcandidatereportDomain } from '../selectors';

describe('selectClientcandidatereportDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
