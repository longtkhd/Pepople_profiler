// import { selectRecruiterInviteDomain } from '../selectors';

describe('selectRecruiterInviteDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
