// import { selectRecruiterListPageDomain } from '../selectors';

describe('selectRecruiterListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
