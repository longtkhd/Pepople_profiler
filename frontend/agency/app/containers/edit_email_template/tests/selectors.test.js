// import { selectEditEmailTemplateDomain } from '../selectors';

describe('selectEditEmailTemplateDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
