/*
 * CandidateReportPage Messages
 *
 * This contains all the text for the CandidateReportPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CandidateReportPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CandidateReportPage container!',
  },
});
