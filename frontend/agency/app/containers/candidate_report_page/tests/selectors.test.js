// import { selectCandidateReportPageDomain } from '../selectors';

describe('selectCandidateReportPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
