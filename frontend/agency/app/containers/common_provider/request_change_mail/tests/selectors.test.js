// import { selectRequestChangeMailDomain } from '../selectors';

describe('selectRequestChangeMailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
