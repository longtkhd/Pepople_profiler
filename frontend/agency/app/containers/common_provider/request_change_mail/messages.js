/*
 * RequestChangeMail Messages
 *
 * This contains all the text for the RequestChangeMail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RequestChangeMail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RequestChangeMail container!',
  },
});
