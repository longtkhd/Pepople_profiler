// import { selectCreateNewJobDomain } from '../selectors';

describe('selectCreateNewJobDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
