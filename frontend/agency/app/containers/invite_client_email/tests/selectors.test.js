// import { selectInviteClientEmailDomain } from '../selectors';

describe('selectInviteClientEmailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
