/*
 * NotificationItem Messages
 *
 * This contains all the text for the NotificationItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NotificationItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NotificationItem component!',
  },
});
