// import { selectNotificationListPageDomain } from '../selectors';

describe('selectNotificationListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
