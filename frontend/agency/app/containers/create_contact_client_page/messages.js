/*
 * CreateContactClientPage Messages
 *
 * This contains all the text for the CreateContactClientPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CreateContactClientPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateContactClientPage container!',
  },
});
