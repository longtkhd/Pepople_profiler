// import { selectInviteAssessmentEmailDomain } from '../selectors';

describe('selectInviteAssessmentEmailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
