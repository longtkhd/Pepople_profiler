// import { selectClientListPageDomain } from '../selectors';

describe('selectClientListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
