/*
 * RecruiterEmailSettings Messages
 *
 * This contains all the text for the RecruiterEmailSettings container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RecruiterEmailSettings';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RecruiterEmailSettings container!',
  },
});
