// import { selectRecruiterEmailSettingsDomain } from '../selectors';

describe('selectRecruiterEmailSettingsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
