// import { selectInviteAllClientEmailDomain } from '../selectors';

describe('selectInviteAllClientEmailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
