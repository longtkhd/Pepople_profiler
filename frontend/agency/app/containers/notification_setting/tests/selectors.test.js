// import { selectNotificationSettingDomain } from '../selectors';

describe('selectNotificationSettingDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
