// import { selectSubscriptionInfoDomain } from '../selectors';

describe('selectSubscriptionInfoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
