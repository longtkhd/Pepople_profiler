// import { selectCandidateReportPreviewDomain } from '../selectors';

describe('selectCandidateReportPreviewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
