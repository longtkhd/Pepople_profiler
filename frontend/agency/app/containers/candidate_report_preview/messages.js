/*
 * RecruiterListPage Messages
 *
 * This contains all the text for the RecruiterListPage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CandidateReportPreview';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'This is the CandidateMainReport component!',
    },
});
