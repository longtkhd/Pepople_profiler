// import { selectClientDetailPageDomain } from '../selectors';

describe('selectClientDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
