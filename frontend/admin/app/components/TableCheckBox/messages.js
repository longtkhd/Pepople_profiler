/*
 * TableCheckBox Messages
 *
 * This contains all the text for the TableCheckBox component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TableCheckBox';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableCheckBox component!',
  },
});
