/*
 * WrapperHeight Messages
 *
 * This contains all the text for the WrapperHeight component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WrapperHeight';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WrapperHeight component!',
  },
});
