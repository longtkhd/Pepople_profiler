/*
 * AgencyPage Messages
 *
 * This contains all the text for the AgencyPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AgencyPage';

export default defineMessages({
  back: {
    id: `${scope}.back`,
    defaultMessage: 'Back',
  },
});
