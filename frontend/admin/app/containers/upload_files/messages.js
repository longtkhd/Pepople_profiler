/*
 * UploadFiles Messages
 *
 * This contains all the text for the UploadFiles container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UploadFiles';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UploadFiles container!',
  },
});
