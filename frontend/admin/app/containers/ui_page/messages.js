/*
 * UIpage Messages
 *
 * This contains all the text for the UIpage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UIpage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UIpage container!',
  },
});
