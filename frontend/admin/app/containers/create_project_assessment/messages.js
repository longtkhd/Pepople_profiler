/*
 * CreateProjectAssessment Messages
 *
 * This contains all the text for the CreateProjectAssessment container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CreateProjectAssessment';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateProjectAssessment container!',
  },
  addProject: {
    id: `${scope}.addProject`,
    defaultMessage: 'New Project Assessment',
  },
  addNew: {
    id: `${scope}.addNew`,
    defaultMessage: 'Add new',
  },

});
