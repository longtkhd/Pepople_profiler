// import { selectFogotPasswordDomain } from '../selectors';

describe('selectFogotPasswordDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
