// import { selectAdminRecruiterListDomain } from '../selectors';

describe('selectAdminRecruiterListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
