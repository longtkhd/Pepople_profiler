// import { selectEditAssessmentTypeDomain } from '../selectors';

describe('selectEditAssessmentTypeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
