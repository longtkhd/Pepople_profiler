// import { selectCreateAssessmentTypeDomain } from '../selectors';

describe('selectCreateAssessmentTypeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
