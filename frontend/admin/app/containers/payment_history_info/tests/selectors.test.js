// import { selectPaymentHistoryInfoDomain } from '../selectors';

describe('selectPaymentHistoryInfoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
