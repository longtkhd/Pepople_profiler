// import { selectAdminAssessmentTypeDomain } from '../selectors';

describe('selectAdminAssessmentTypeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
