// import { selectProjectAssessmentDomain } from '../selectors';

describe('selectProjectAssessmentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
