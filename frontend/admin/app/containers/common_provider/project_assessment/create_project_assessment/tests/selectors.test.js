// import { selectCreateProjectAssessmentDomain } from '../selectors';

describe('selectCreateProjectAssessmentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
