/*
 * SubscriptionSumary Messages
 *
 * This contains all the text for the SubscriptionSumary container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SubscriptionSumary';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SubscriptionSumary container!',
  },
});
