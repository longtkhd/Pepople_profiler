// import { selectSubscriptionSumaryDomain } from '../selectors';

describe('selectSubscriptionSumaryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
