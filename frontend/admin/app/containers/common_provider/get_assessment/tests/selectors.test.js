// import { selectGetAssessmentDomain } from '../selectors';

describe('selectGetAssessmentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
