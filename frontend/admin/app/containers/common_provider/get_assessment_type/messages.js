/*
 * GetAssessmentType Messages
 *
 * This contains all the text for the GetAssessmentType container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GetAssessmentType';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GetAssessmentType container!',
  },
});
